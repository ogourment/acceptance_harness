#!/bin/sh
set -eu
self=$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$0")
root=$(CDPATH= cd -- "$(dirname -- "$self")/.." && pwd)
exec python3 "$root/priv/preview/review_server.py" "$@"
